/*union data {
    int _enter;
    racional _racional;
    double _real;
    string _variable;
    data();
    data(const data & d) throw();
    data & operator=(const data & d) throw();
    ~data() throw();
};

//void *ptrv; // punter void, el qual podem modificar el tipus;
// per poder guardar els valors que siguin necessaris, 

data _valor;*/

token::codi _id;

void* _valor;

bool check_variables(string var);

int numero_operadors();